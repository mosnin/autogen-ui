# Pick one provider. The API route auto-detects which key is set.
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Optional: override the default model for the chosen provider.
# AUTOGEN_UI_MODEL=claude-sonnet-4-6
